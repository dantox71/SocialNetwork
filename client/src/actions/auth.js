import {
  REGISTER_SUCCESS,
  REGISTER_FAIL,
  LOGIN_SUCCESS,
  LOGIN_FAIL
} from "./types";

export const register = formData => dispatch => {
  //Make request to server and dispatch with register type
  console.log("Registered");
};
